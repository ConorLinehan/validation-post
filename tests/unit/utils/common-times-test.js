import commonTimes from 'validation-post/utils/common-times';
import { module, test } from 'qunit';

module('Unit | Utility | common times');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = commonTimes();
  assert.ok(result);
});
